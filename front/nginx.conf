server {
    listen 80;
    server_name _;

    # 프론트 정적 파일 + SPA 라우팅 담당
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}